{
  "name": "steampunk-platformer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:audio": "node tools/gen/build-audio.mjs",
    "assets:build": "node tools/gen/build-assets.mjs",
    "assets:build:all": "node tools/gen/build-assets-all.mjs",
    "assets:clips": "node tools/gen/build-clips.mjs",
    "assets:levels": "node tools/gen/make-levels.mjs",
    "assets:prompts": "node tools/gen/write-prompts.mjs",
    "assets:world": "node tools/gen/build-world.mjs",
    "build": "tsc --noEmit && tsc --noEmit -p tsconfig.build.json && vite build && node tools/gen/verify-dist.mjs",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "npm run build && node tools/dev/free-port.mjs 5173 && node tools/dev/free-port.mjs 4173 && playwright test",
    "test:sim-isolated": "npm rm phaser && vitest run && npm i phaser@4.2.1 --save-exact && node -e \"require('fs').rmSync('node_modules/.vite',{recursive:true,force:true})\"",
    "typecheck": "tsc --noEmit",
    "typecheck:build": "tsc --noEmit -p tsconfig.build.json"
  },
  "devDependencies": {
    "@babel/parser": "8.0.4",
    "@playwright/test": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "phaser": "4.2.1"
  }
}

/**
 * Typed view of `promptData.mjs`. Re-exported through `prompt.d.mts`'s sibling module boundary —
 * this file exists because every `tools/gen/*.mjs` has a hand-written `.d.mts` sibling, even
 * though no `.ts` file currently imports these two tables directly.
 */

export declare const SHEET_PHASES: Readonly<Record<string, readonly string[]>>;
export declare const ANCHOR_CONCEPTS: Readonly<Record<string, string>>;
